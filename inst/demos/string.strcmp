%!demo
%! ## `strcmp` compares strings for exact equality — the same idea as `==`, kept
%! ## for compatibility with the classic string functions.
%!
%! a = string ({'cat'; 'dog'})
%! strcmp (a, 'cat')

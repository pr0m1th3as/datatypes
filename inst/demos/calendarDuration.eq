%!demo
%! ## `eq` (`==`) tests calendar durations for equality.  Equivalent spans compare
%! ## equal even when written with different builders.
%!
%! calmonths (12) == calyears (1)
%! calweeks (2) == caldays (14)

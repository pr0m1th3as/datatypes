%!demo
%! ## `append` joins its arguments end to end, element-wise — like `strcat`, but it
%! ## keeps trailing whitespace.
%!
%! first = string ({'Ann'; 'Bob'})
%! append (first, ' ', string ({'Lee'; 'Ng'}))
